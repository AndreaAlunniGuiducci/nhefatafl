import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mainButton: {
    padding: 15,
    borderWidth: 1,
    borderRadius: 20,
  },
  buttonContent: {
    fontFamily: "Unbounded-Light",
    fontSize: 20,
  },
});
